# Environment variables for local development and CI/CD

# Claude API (required for running pyromai)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub Token (required for semantic-release to push commits/tags)
# Create at: https://github.com/settings/tokens/new with 'repo' + 'workflow' scopes
GH_TOKEN=ghp_...

# PyPI Tokens (required for publishing to PyPI registries)
# Create at: https://pypi.org/manage/account/tokens/
PYPI_API_TOKEN=pypi-...

# TestPyPI Token (required for testing before production release)
# Create at: https://test.pypi.org/manage/account/tokens/
TESTPYPI_API_TOKEN=pypi-...
